#pragma once

#include <inttypes.h>
#include <stdbool.h>

namespace Render
{

// ShaderSet is used bind shader resources (image view, buffer view, UAV)
class ShaderReflection
{
};
}; // namespace Render
