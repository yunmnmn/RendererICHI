#pragma once

#include <inttypes.h>
#include <stdbool.h>

namespace Render
{
class Renderer
{
   Renderer();
   ~Renderer();

   void Initialize(const char* p_appName);

}; // namespace Render
} // namespace Render
